<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="KikuzukiWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KikuzukiWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="KikuzukiWinUI">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <StackPanel Orientation="Vertical">
            <Viewbox x:Name="ImageViewbox" MaxHeight="480">
                <Grid
                    x:Name="PaneGrid"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    CornerRadius="{ThemeResource OverlayCornerRadius}">
                    <Image x:Name="ImgCamera" Stretch="Uniform"/>
                    <Canvas
                        x:Name="RectCanvas"
                        Background="{ThemeResource SmokeFillColorDefaultBrush}"
                        Visibility="Collapsed" />
                </Grid>
            </Viewbox>
            <StackPanel Orientation="Horizontal">
                <ComboBox
                  x:Name="ComboBoxCamera"
                  SelectionChanged="ComboBoxCameraChanged"
                  Margin="5"
                  Width="185"
                  Height="35"
                  SelectedIndex="0">
                </ComboBox>
                <Button x:Name="ButtonCamera" Click="ButtonCameraClick" Width="185" Height="35" Margin="5" HorizontalAlignment="Left">
                    <TextBlock x:Name="ButtonCameraText">Camera</TextBlock>
                </Button>
                <Button x:Name="ButtonClear" Click="ButtonClearClick" Width="185" Height="35" Margin="5" HorizontalAlignment="Left" IsEnabled="False">
                    <TextBlock x:Name="ButtonClearText">Clear</TextBlock>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="ButtonRecognize" Click="ButtonRecognizeClick" Width="185" Height="35" Margin="5" HorizontalAlignment="Left" IsEnabled="False">
                    <TextBlock x:Name="ButtonRecognizeText">Describe</TextBlock>
                </Button>
                <Button x:Name="ButtonOCR" Click="ButtonOCRClick" Width="185" Height="35" Margin="5" HorizontalAlignment="Left" IsEnabled="False">
                    <TextBlock x:Name="ButtonOCRText">Recognize</TextBlock>
                </Button>
            </StackPanel>
            <TextBox x:Name="TextRecognized" Width="600" Height="100" Margin="5" HorizontalAlignment="Left" TextWrapping="Wrap" IsReadOnly="True" />
        </StackPanel>
    </Grid>
</Window>
